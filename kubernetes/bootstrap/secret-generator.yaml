apiVersion: viaduct.ai/v1
kind: ksops

metadata:
  name: sops-secret-generator

  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops

files:
  - secrets/argocd-secret.sops.yaml
  - secrets/cloudflare-api-token.sops.yaml
  - secrets/ghcr-token.sops.yaml
  - secrets/sops-age-key.sops.yaml
